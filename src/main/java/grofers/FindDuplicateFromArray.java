package grofers;

import java.util.Arrays;

/**
 * Find all duplicate elements from Array with space O(1) and runtime O(n)
 * Elements are between 1-n, n is size of the array
 */
public class FindDuplicateFromArray {

    private static void findDuplicate(final int[] nums) {
        final int len = nums.length;

        int[] temp = nums.clone();
        for (int i : temp) {
            if (temp[i] > 0) {

            }
        }
    }

    public static void main(String[] args) {

    }

}
